import React from 'react';
import FileTreeMenu from './menu';
import Tree from './tree';

const FileTree = () => {
  return (
    <div>
      <FileTreeMenu />
      <Tree />
    </div>
  );
};

export default FileTree;
